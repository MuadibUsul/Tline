# Tline 上线部署计划

- 建立时间：2026-09-02 20:27（Asia/Shanghai）
- 目标架构：GitHub Actions 构建镜像 → GHCR → VPS 拉取运行
- VPS：4 核 / 6GB，**与其他 3–4 个项目共用同一台机器**
- 域名：已有，Caddy 自动签发 Let's Encrypt 证书
- 认证：公开内容免登录；进阶功能用**邮件魔术链接**注册

---

## 0. 阻塞项：上线前必须完成的代码改动

这四项不做完，`npm run env:check:production` 过不了，或者上线即出问题。

### 0.1 邮件魔术链接登录（未实现，最大的一块）

现状：`auth-config.ts` 只支持 `azure-ad` 和 `google`。你要的「邮箱注册 + 发确认链接」是 NextAuth 的 **EmailProvider**，代码里没有。

好消息是地基已经在了：
- `VerificationToken` 模型已存在（schema.prisma:648）
- `PrismaAdapter` 已接入
- `session.strategy = "database"` 与 EmailProvider 兼容

需要做的：
1. `npm i nodemailer`（NextAuth v4 的 EmailProvider 依赖它，当前未安装）
2. `auth-config.ts`：`selectedProvider()` 增加 `"email"` 分支，`providers()` 里加 `EmailProvider({ server: process.env.EMAIL_SERVER, from: process.env.EMAIL_FROM })`
3. `isFormalAuthConfigured()` 对 email 模式返回 `Boolean(EMAIL_SERVER && EMAIL_FROM)`
4. `src/app/signin/page.tsx`：加邮箱输入框，提交到 `signIn("email", { email })`
5. `scripts/validate-env.mjs`：`AUTH_PROVIDER=email` 时校验 `EMAIL_SERVER` / `EMAIL_FROM`
6. `docker-compose.yml`：app 服务透传 `EMAIL_SERVER`、`EMAIL_FROM`

> ⚠️ **不要用 VPS 自建 SMTP 发信。** 新 VPS 的 IP 几乎必然在各家的低信誉段，魔术链接会进垃圾箱或被直接拒收——而这个链接就是登录本身，进不了收件箱等于登录功能不可用。必须用事务邮件服务商（Resend / Postmark / AWS SES / 阿里云邮件推送），并配好域名的 SPF + DKIM。**这是一个需要你决定的采购项。**

### 0.2 移除 `ports: 3000:3000`

`docker-compose.yml:44` 把 app 直接暴露到宿主机 0.0.0.0:3000。共享机器上这等于绕过 Caddy 裸奔，且和其他项目抢端口。改为只接入 Caddy 所在的共享网络，不发布端口。

### 0.3 内存限制与并发下调

共用机器上任何一个服务 OOM 都会拖垮邻居。必须给每个容器 `mem_limit`，并把采集并发从默认 8 降下来（`INGEST_CONCURRENCY` 默认值见 run.ts:546）。

### 0.4 合并分支

`fix/platform-audit-remediation` 还没合。上线前先合进 `main`，因为部署流水线以 `main` 为触发源。

---

## 1. 资源预算（6GB / 4 核，多项目共存）

Chromium 动态渲染是唯一的内存尖峰。研究调度器会 spawn 子进程跑 Chromium，单实例 300–500MB。

| 服务 | mem_limit | 说明 |
|---|---|---|
| `db` (postgres:16) | 512M | `shared_buffers=128MB`，`max_connections=50` |
| `app` (Next) | 768M | |
| `scheduler` | 1G | Chromium 尖峰在这里 |
| `macro-scheduler` | 384M | 无浏览器 |
| **Tline 小计** | **~2.6G** | |
| Caddy（全机共享） | 64M | 所有项目共用一个 |

给其他 3 个项目留约 3.3GB，OS 约 400MB。**务必加 4GB swap**——共享机器上这是最便宜的保险。

配套参数：
```
INGEST_CONCURRENCY=2          # 默认 8，共享机上过高
INGEST_ARTICLE_LIMIT=4
TRANSLATION_CONCURRENCY=2
REPARSE_CONCURRENCY=2
MACRO_MARKET_SYNC_INTERVAL_MS=1800000   # 已是默认；TwelveData 免费层 800/天
```

---

## 2. VPS 基线（一次性）

```bash
# 非 root 部署用户
adduser deploy && usermod -aG sudo deploy
rsync --archive --chown=deploy:deploy ~/.ssh /home/deploy

# SSH 加固：禁用密码与 root 登录
sudo sed -i 's/^#\?PasswordAuthentication.*/PasswordAuthentication no/' /etc/ssh/sshd_config
sudo sed -i 's/^#\?PermitRootLogin.*/PermitRootLogin no/' /etc/ssh/sshd_config
sudo systemctl restart ssh

# 防火墙：只开 22/80/443
sudo ufw allow OpenSSH && sudo ufw allow 80 && sudo ufw allow 443 && sudo ufw enable

# swap 4GB
sudo fallocate -l 4G /swapfile && sudo chmod 600 /swapfile
sudo mkswap /swapfile && sudo swapon /swapfile
echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab
sudo sysctl -w vm.swappiness=10

# Docker
curl -fsSL https://get.docker.com | sudo sh
sudo usermod -aG docker deploy

# 全局日志上限，防止任一项目日志撑爆磁盘
sudo tee /etc/docker/daemon.json <<'JSON'
{ "log-driver": "json-file", "log-opts": { "max-size": "10m", "max-file": "3" } }
JSON
sudo systemctl restart docker
```

---

## 3. 共享 Caddy（全机一份，不属于任何单个项目）

```bash
docker network create web    # 所有项目共用的前置网络
mkdir -p ~/proxy && cd ~/proxy
```

`~/proxy/docker-compose.yml`：
```yaml
services:
  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    mem_limit: 64m
    ports: ["80:80", "443:443"]
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config
    networks: [web]
networks:
  web:
    external: true
volumes:
  caddy-data:
  caddy-config:
```

`~/proxy/Caddyfile`：
```
tline.example.com {
    encode zstd gzip
    reverse_proxy tline-app:3000
}
# 其他项目按同样格式往下加
```

> Caddy 自动申请和续期证书，无需 cron。DNS 的 A 记录必须先指向 VPS，否则签发会失败。

---

## 4. 生产 compose 改造

新增 `docker-compose.prod.yml`（覆盖开发版的 `build:` 和 `ports:`）：

```yaml
services:
  db:
    mem_limit: 512m
    command: >
      postgres -c shared_buffers=128MB -c effective_cache_size=384MB
               -c max_connections=50 -c work_mem=4MB
    networks: [default]

  app:
    image: ghcr.io/muadibusul/tline:${TAG:-latest}
    build: !reset null
    container_name: tline-app        # Caddyfile 按这个名字反代
    mem_limit: 768m
    ports: !reset []                 # 不再暴露到宿主机
    environment:
      SITE_URL: ${SITE_URL}
      NEXTAUTH_URL: ${SITE_URL}
      AUTH_PROVIDER: email
      EMAIL_SERVER: ${EMAIL_SERVER}
      EMAIL_FROM: ${EMAIL_FROM}
      HEALTH_DETAIL_TOKEN: ${HEALTH_DETAIL_TOKEN}
    networks: [default, web]

  scheduler:
    image: ghcr.io/muadibusul/tline:${TAG:-latest}
    build: !reset null
    mem_limit: 1g

  macro-scheduler:
    image: ghcr.io/muadibusul/tline:${TAG:-latest}
    build: !reset null
    mem_limit: 384m

networks:
  web:
    external: true
```

启动命令统一为：
```bash
docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
```

> 三个服务共用同一个镜像，`app` 的 CMD 里已经串了 `env:check:production && db:postgres:deploy && npm start`，迁移只由 app 执行，两个调度器 `depends_on: app: service_healthy`，不会并发跑迁移。

---

## 5. 密钥与环境变量

VPS 上 `~/tline/.env`（权限 600，永不进 git）：

```bash
cd ~/tline
umask 077
cat > .env <<'ENV'
POSTGRES_PASSWORD=<openssl rand -base64 24>
AUTH_SECRET=<openssl rand -base64 48>
SITE_URL=https://tline.example.com
ADMIN_EMAILS=carlmuadib@gmail.com
HEALTH_DETAIL_TOKEN=<openssl rand -hex 24>

AUTH_PROVIDER=email
EMAIL_SERVER=smtp://apikey:<ESP_KEY>@smtp.provider.com:587
EMAIL_FROM="Tline <no-reply@tline.example.com>"

DOCUMENT_STORAGE_DRIVER=local
DOCUMENT_STORAGE_ROOT=/app/storage

DEEPSEEK_API_KEY=...
FRED_API_KEY=...
BEA_API_KEY=...
EIA_API_KEY=...
BLS_API_KEY=...
TWELVE_DATA_API_KEY=<轮换后的新 key>
TWELVE_DATA_QUALITY=DELAYED

INGEST_CONCURRENCY=2
INGEST_ARTICLE_LIMIT=4
TRANSLATION_CONCURRENCY=2
REPARSE_CONCURRENCY=2
ENV
chmod 600 .env
```

生产校验会强制：`DATABASE_URL` 必须 postgres、`AUTH_SECRET` ≥32 且不含 `dev/insecure/change-me`、`SITE_URL` 必须 https、`HEALTH_DETAIL_TOKEN` ≥24 字符、`DOCUMENT_STORAGE_ROOT` 必须绝对路径。

---

## 6. 首次部署（手动走一遍，确认无误再上自动化）

```bash
cd ~/tline
git clone https://github.com/MuadibUsul/Tline.git .   # 或只传两个 compose 文件
echo $GHCR_TOKEN | docker login ghcr.io -u MuadibUsul --password-stdin

docker compose -f docker-compose.yml -f docker-compose.prod.yml pull
docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d

docker compose logs -f app          # 确认 env:check 和 migrate deploy 通过
docker compose exec app npm run db:seed
docker compose exec app npx tsx scripts/market-sync.ts --from=2026-01-01
docker compose exec app npm run forecasts
```

验证：
```bash
curl -s https://tline.example.com/api/health                     # 应只返回 {"status":"ok"}
curl -s -H "Authorization: Bearer $HEALTH_DETAIL_TOKEN" \
     https://tline.example.com/api/health | head -c 300          # 应返回完整详情
curl -sI https://tline.example.com | grep -i content-security    # 安全头
curl -s https://tline.example.com/robots.txt                     # Host 应是真实域名
```

---

## 7. GitHub Actions 自动化

**为什么不在 VPS 上构建**：`npm ci` + `next build` 的峰值内存会和另外 3 个项目抢，6GB 机器上容易 OOM。构建放 Actions（免费额度足够），VPS 只做 `pull` + `up`。

新增 `.github/workflows/deploy.yml`：

```yaml
name: Deploy
on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy-production
  cancel-in-progress: false

jobs:
  verify:
    uses: ./.github/workflows/ci.yml     # 复用已有 CI，不重复定义

  build:
    needs: verify
    runs-on: ubuntu-latest
    permissions: { contents: read, packages: write }
    outputs:
      tag: ${{ steps.meta.outputs.tag }}
    steps:
      - uses: actions/checkout@v4
      - id: meta
        run: echo "tag=sha-${GITHUB_SHA::12}" >> $GITHUB_OUTPUT
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/tline:${{ steps.meta.outputs.tag }}
            ghcr.io/${{ github.repository_owner }}/tline:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment: production          # 可加人工审批
    steps:
      - name: Deploy over SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            set -euo pipefail
            cd ~/tline
            export TAG=${{ needs.build.outputs.tag }}
            echo "$TAG" > .deploy-tag
            docker compose -f docker-compose.yml -f docker-compose.prod.yml pull
            docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d

            # 只保留最近 3 个镜像。sha- 标签让 docker image prune 永远删不掉旧镜像
            # （prune 只删无标签的悬空镜像），不清理的话每次部署堆积约 1.4GB 独占层。
            # 不要用 prune -af：机器上还有别的项目，-a 会删掉它们的回滚镜像，
            # 而 prune 不支持 reference 过滤，没法只针对一个项目。
            docker image ls --filter=reference='ghcr.io/muadibusul/tline:sha-*' --format '{{.ID}} {{.CreatedAt}}' | sort -k2 -r | tail -n +4 | awk '{print $1}' | xargs -r docker rmi || true

            # 健康门禁：90 秒内不 ok 就回滚到上一个镜像
            for i in $(seq 1 18); do
              if curl -fsS localhost/api/health -H "Host: ${{ secrets.SITE_HOST }}" | grep -q '"status"'; then
                echo "healthy"; exit 0
              fi
              sleep 5
            done
            echo "unhealthy, rolling back"
            export TAG=$(cat .deploy-tag.prev 2>/dev/null || echo latest)
            docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
            exit 1
```

需要在 GitHub 仓库设置的 Secrets：
- `VPS_HOST`、`VPS_USER`（deploy）、`VPS_SSH_KEY`（专用部署密钥，不要复用个人密钥）
- `SITE_HOST`（域名，健康检查用）
- GHCR 用内置 `GITHUB_TOKEN`，无需额外配置

> 部署密钥在 VPS 上用 `command=` 限制或至少单独生成一把，泄露时可以单独吊销。

---

## 7.1 镜像保留（必须，否则磁盘会被填满）

部署流水线用 `sha-<commit>` 标签是为了能回滚到指定版本，但这个标签有个副作用：**`docker image prune -f` 永远清不掉旧镜像**，因为它只删悬空（无标签）镜像。新部署推 `latest` 时，标签会转移到新镜像，旧镜像失去 `latest` 却仍留着自己的 `sha-`，于是不悬空、不被回收。

实测代价：26 小时 23 次部署 → 22 个陈旧镜像 → **31.8 GB 可回收**。

> 注意 `docker images` 的 SIZE 列（4.6 GB）是**虚拟大小**，重复计算了共享层。23 × 4.6 ≠ 106 GB。以 `docker system df` 的 RECLAIMABLE 为准：每个陈旧镜像的独占数据约 1.4 GB。

上面 deploy 步骤里已经加了保留最近 3 个的清理。存量先手动清一次：

```bash
docker system df                      # 先看真实占用
docker image ls --filter=reference='ghcr.io/muadibusul/tline:sha-*' --format '{{.ID}} {{.CreatedAt}}' | sort -k2 -r | tail -n +4 | awk '{print $1}' | xargs -r docker rmi
docker system df                      # 确认回收量
```

运行中的镜像不用担心：不带 `-f` 的 `docker rmi` 对使用中的镜像会直接拒绝。

**另外三个项目如果用了同样的标签模式，多半也在堆积**，一并检查。

## 7.2 镜像瘦身（次要，先做 7.1）

保留策略回收 31.8 GB；镜像瘦身最多再省 1 GB 左右（约 350 MB × 保留 3 个）。**先做 7.1，7.2 是可选项。**

已实测、已排除的方案：

| 方案 | 结论 |
|---|---|
| 多阶段构建 / 砍 devDependencies | **不值得**。实测只省约 35 MB（eslint 3 + typescript 23 + @types 3 + @typescript-eslint 5）。`prisma` 29 MB、`@prisma` 81 MB、`tsx`+`@esbuild` 12 MB 都是运行时真依赖——调度器 spawn `tsx prisma/reparse.ts`，容器启动跑 `prisma migrate deploy`。 |
| 删除 Chromium | **不可行**。13 个启用中的源需要动态渲染，含 Goldman Sachs、Morgan Stanley、PIMCO、Saxo、Charles Schwab，且多数在 09-01～09-03 刚成功抓取。删了会静默丢源。 |

值得做的两项：

**a) 清理 npm 缓存（零风险）** — `npm ci` 会在 `/root/.npm` 留下缓存并固化进镜像层。改 Dockerfile 同一个 RUN 内清掉：

```dockerfile
RUN npm ci && npm cache clean --force
```

**b) CJK 字体（约 330 MB，但有取舍）** — `fonts-noto-cjk` 装的是 `.ttc` 字体集合，同时包含日文、韩文、简中、繁中四套字形，所以体积大。PDF 生成只用到 `NotoSansCJK-Regular.ttc` / `-Bold.ttc`。

改成只装简中版 Noto（约 10 MB）可省约 320 MB，**但这是行为变更**：如果任何研报正文含日文或韩文，PDF 里会出现豆腐块。当前语料是英文→简中，大概率安全，但需要你确认后再改，我没有擅自动。

**先测再改** —— 我在本地没能构建（Docker Desktop 的 Linux 引擎未运行），所以没有分层实测数据。在 VPS 上跑这个拿真实分布：

```bash
docker history ghcr.io/muadibusul/tline:latest --human --format '{{.Size}}	{{.CreatedBy}}' | head -12
docker system df -v | head -30
```

## 8. 备份与监控

**备份**（`scripts/backup.mjs` 已实现，Postgres 走 `pg_dump --format=custom`）：

```bash
# crontab -e  每天 03:30
30 3 * * * cd /home/deploy/tline && docker compose exec -T app npm run backup -- --out /app/storage/backups --keep 14 >> /var/log/tline-backup.log 2>&1
```

⚠️ 快照落在 `document-storage` 卷里，**和数据在同一台机器上，不算异地备份**。VPS 挂了就一起没了。加一步同步到对象存储（rclone / aws s3 sync），并定期用 `npm run backup:verify -- <snapshot>` 校验——没验证过的备份不算备份。

**监控**：用 UptimeRobot / Better Stack 之类探测 `https://tline.example.com/api/health`，匿名返回只有 `{status}`，不泄露内部信息。想看详情就带 `Authorization: Bearer $HEALTH_DETAIL_TOKEN`。`status=degraded` 表示 worker 心跳陈旧或有任务卡死。

---

## 9. 上线检查清单

- [ ] `fix/platform-audit-remediation` 已合入 main
- [ ] 邮件登录已实现并**实测收到过魔术链接**（不是只看代码通过）
- [ ] SPF + DKIM 已配置，用 mail-tester 之类测过分数
- [ ] TwelveData key 已轮换（旧 key 曾出现在对话记录里）
- [ ] `docker compose config` 确认 app 没有 `ports`
- [ ] `env:check:production` 无 ERROR
- [ ] 匿名 `/api/health` 只返回 `{status}`
- [ ] 安全响应头存在（CSP / X-Frame-Options / HSTS）
- [ ] `robots.txt` 的 Host 是真实域名而非 localhost
- [ ] `sitemap.xml` 可访问
- [ ] 手机 390px 下导航抽屉可用
- [ ] 备份跑通一次并 verify 通过
- [ ] 异地备份同步已配置
- [ ] `docker stats` 确认四个容器内存在预算内
- [ ] 其他项目未受影响

---

## 10. 未决事项（需要你决定）

1. **事务邮件服务商** — 魔术链接的送达率直接决定注册功能是否可用。VPS 自发 SMTP 不可行。
2. **异地备份目标** — 对象存储桶 / 另一台机器，我不知道你有什么。
3. **指数授权**（SPX / NDX）— 之前搁置的第 4 步，不影响上线。
4. **免责声明与服务条款** — 站点公开转载机构研究并附 AI 生成解读，从私有预览转为公网站点后这块的暴露面不同了。当前只有 footer 一行 "Phase-1 · verified public research"，研报页有机构原始免责声明，但没有站点自身的 ToS / 免责页。这是产品和法务判断，不是技术问题。

---

## 建议执行顺序

1. 合分支
2. 实现邮件登录（含 ESP 选型）+ compose 三项改造（移除 ports、mem_limit、并发下调）
3. VPS 基线 + 共享 Caddy
4. 手动部署一次跑通
5. 上 Actions 自动化
6. 备份 + 监控
7. 走检查清单，开放访问

第 2 步是唯一有实质代码量的部分，其余是配置和运维。
