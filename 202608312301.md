# Tline 全站优化执行记录

- 建立时间：2026-08-31 23:01（Asia/Shanghai）
- 执行原则：先修可靠性和可信度，再修体验与性能；保留现有公开发布策略，不重新引入 `needs_review` 硬拦截。
- 状态标记：`[ ]` 待执行、`[~]` 执行中、`[x]` 已完成、`[!]` 受外部条件阻塞。

## 当前基线

- 295 篇文章均有分析和中文翻译；31 篇分析为 `needs_review`。
- 169 篇译文质量分低于 0.8，平均质量分约 0.66。
- 62 个机构中，21 个最近状态为成功、27 个暂停、8 个运行中；14 个启用来源超过 24 小时未成功。
- `/institutions` 本地开发环境响应约 0.8 秒；搜索冷请求约 1.46 秒。
- 移动端 390px 下主导航全部不可见。
- Docker 镜像未安装 Chromium，动态渲染仅在本机 Windows 可用。
- 健康接口未把调度心跳陈旧或任务卡死计入 unhealthy。
- 当前单元测试：136 项通过；生产构建通过。

## 执行计划

### A. 生产采集与任务可靠性

- [x] A1. 在生产镜像安装 Chromium，并为动态渲染配置稳定路径。
- [x] A2. 为研究调度器和宏观调度器写入数据库心跳。
- [x] A3. 健康检查识别陈旧心跳、超时运行任务和降级来源。
- [x] A4. 让容器健康检查覆盖 worker，而不只检查 Web 应用。
- [~] A5. 后台展示熔断、暂停、卡死和失败原因聚合。

### B. 内容可信度

- [ ] B1. 明确区分“AI 观点摘要”和“英文证据”，避免被理解为直译。
- [ ] B2. 增加观点与证据的确定性一致性检查，拒绝无依据新增数字、实体和绝对化表述。
- [ ] B3. 分析 `needs_review` 和低质量译文保持公开但显示质量提示，不重新设置发布关卡。
- [ ] B4. 为低质量内容提供异步重试入口，并记录重试结果。
- [ ] B5. 重新处理现有 31 篇异常分析与 169 篇低分译文；若模型凭据不可用则记录阻塞。

### C. 移动端与阅读体验

- [ ] C1. 增加可访问的移动端导航菜单，覆盖全部主入口和账户操作。
- [ ] C2. 改善长篇研报段落、章节间距、正文行宽与原文/译文切换说明。
- [ ] C3. 统一按钮和可点击卡片的按压反馈，保留键盘焦点与 reduced-motion。
- [ ] C4. 移动端验证首页、研报、观点、宏观、监控和运营页面。

### D. 查询与搜索性能

- [ ] D1. 观点流在数据库层只读取最近 7 天，避免加载全部历史观点。
- [ ] D2. 为搜索索引增加数据版本失效机制，避免固定 60 秒全量重建。
- [ ] D3. 为大规模数据准备数据库全文检索迁移边界；本轮优先消除当前冷启动重复加载。
- [ ] D4. 记录主要页面与搜索优化前后响应时间。

### E. 安全、运维与站点基础

- [ ] E1. 将公开健康接口缩减为无敏感信息摘要，详细信息仅管理员可见。
- [ ] E2. 增加 CSP、Frame、Referrer、Content-Type 等基础安全响应头。
- [ ] E3. 增加站点级 robots、sitemap、图标和页面级 metadata。
- [ ] E4. 增加数据库与文档卷的一致性备份脚本、校验清单和恢复说明。
- [ ] E5. 为搜索接口增加轻量限流，避免昂贵查询被滥用。

### F. 自动验证

- [ ] F1. 增加移动端导航、搜索、研报详情、健康接口权限的浏览器回归检查。
- [ ] F2. 运行全量单元测试、类型检查和生产构建。
- [ ] F3. 重启本地服务与调度器，核验健康状态和任务持续推进。
- [ ] F4. 汇总改动、验证结果、未完成项和需要外部配置的事项。

## 进度日志

- 2026-08-31 23:01：完成只读审计并建立实施计划；开始 A1。
- 2026-08-31 23:03：Docker 镜像加入 Chromium 和固定执行路径；两个 worker 加入 30 秒数据库心跳与独立容器健康检查，准备迁移本地数据库并验证。
- 2026-08-31 23:05：完成 WorkerHeartbeat schema、本地 db push 和 PostgreSQL migration；前台及两个调度器重启。健康接口显示 research/macro 均为 ok，陈旧运行任务为 0。

## 外部依赖与边界

- 外部 webhook 地址、生产域名、S3 生命周期策略和云端备份目标不能凭空生成；代码和配置入口会完成，缺少具体值时标记为外部配置项。
- 低质量内容重跑依赖已配置的模型凭据；不会伪造重跑成功。
- 不绕过登录、验证码、访问墙或网站 robots 策略。
