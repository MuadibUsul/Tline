# 后续计划 · plan20260827

> Institutional Intelligence（`E:\Codes\Tline`）· 续作清单。
> 目标主线：**Research → Data → Consensus → Signal**。本次会话重点已落在**合规采集 + 清洗 + 分段解析**。

---

## 0. 现状快照（截至 2026-08-27）

**能跑通的**（Next.js + Prisma/SQLite + 全 TS）：
- 首页 Market Consensus + Feed、`/asset/[ticker]`、`/institution/[slug]`、`/research/[id]`、`/markets`、`/institutions`、`/consensus`、`/search`（AI Research）、`/watchlist`、`/alerts`、`/signin`。
- Consensus 引擎（权威加权 + 时间衰减 + 快照）、Alerts（规则即时评估）、AI Research（结构化意图路由，可选 LLM 合成）、目标价历史/View-Change、Cookie 会话鉴权 + Server Actions 交互。
- **数据现状**：demo 种子 + 少量真实抓取混合。真实 tag 质量受 mock 解析器限制。

**合规采集（已完成，严格 robots）**：
- `scripts/robots_audit.py` → `64机构爬虫合规评估.xlsx` + `data/crawl_policy.json`。
- 结论：**可采集 59**（55 allowed + 4 delayed：danske/socgen/swedbank/citi）、robots 禁止 1（janus）、需人工 4（blackrock/neuberger-berman/mizuho/of-america，站点主动封锁/人机验证，**不绕过**）。
- `institutions.crawlPolicy` + `crawlDelay` 已由 `seed.ts` 从 `crawl_policy.json` 回填。
- 采集器 `run.ts`：只跑 allowed/delayed + **运行时 robots 校验**（`src/lib/ingest/robots.ts`：User-agent 分组、最长匹配 Allow/Disallow、`*`/`$`、Crawl-delay）+ 每 URL 逐条校验。
- 清洗：`extract.ts`（样板剔除 + 段落密度选正文）、`isJunk`/`looksLikeArticle` 落库前拦截、别名词边界匹配、打标签评分+封顶（parseLLM.ts）。

**已知限制（真实现状，非 bug）**：
- 静态 HTML 抓取对 **JS 渲染站点**收效低（ING 空/被挡；Saxo 文章正文是 JS 壳 → 被清洗闸正确拒绝）。服务端渲染站点（JPM）抽得很干净。
- **Mock 解析器只有一个全局方向** → 多资产文章会误标（gold 看多但提到 oil/fed）。真实每资产方向需 LLM。

---

## 1. 进行中（本次未完成）：Segment-aware 解析器

**目标（用户方案）**：存完整正文 → 有小标题按段拆 → 逐段识别资产+方向 → 某资产各段方向冲突则**放弃打方向**交 LLM；无小标题则（多资产时）只给主资产方向、其余交 LLM。

**已改动**：
- `schema.prisma`：`Article.rawText String?`（完整正文，内部保存，**前端不展示全文**）。⚠️ **待 `npm run db:push`**。
- `extract.ts`：`extractArticle` 现返回 `segments: {heading, text}[]`（按 h2/h3/h4 拆段）+ `ExtractedArticle` 类型。已完成、可编译。

**待完成（精确步骤）**：
1. **`parseLLM.ts` 重写 `mockParse` → `heuristicParse(input, segments)`**：
   - 候选资产：标题命中或正文≥2 次，按分数封顶 4。
   - `hasHeadings = segments 中存在 heading != null`。
   - 若 `hasHeadings`：逐段对每个候选资产 `aliasHit(段) → detectDirection(段)` 收集非零方向；
     - 方向一致 → 赋该方向（strong 关键词决定强度）；
     - 只有中性 → 0；
     - **同时出现多空 → 标记 unresolved（不赋方向）**。
   - 若 `!hasHeadings`：只给**主资产**（最高分/标题命中）全局方向；其余候选 → unresolved。
   - `needsLLM = unresolved.length > 0`；`reviewStatus = needsLLM ? "needs_review" : "ok"`。
   - 返回 `{ assets: 已解析(带方向), unresolvedTickers, needsLLM, summary, ... }`。
2. **`parseArticle(input, segments)`**：
   - 先 `heuristicParse`。
   - 若 `ANTHROPIC_API_KEY` 且 `needsLLM` → `realParse(全文)` 覆盖返回（LLM 正确分类每资产方向）。
   - 否则 → 返回 heuristic 结果；**unresolved 资产不写 article_assets**（不污染 consensus），文章标 `needs_review`。
3. **`store.ts`**：`RawArticle` 加 `segments?` 与完整正文；`persistArticle` 落 `rawText`；调用 `parseArticle(input, segments)`；只对已解析资产建 `article_assets`。
4. **`run.ts`**：把 `extractArticle` 的 `segments` 塞进 raw 传下去。
5. **`demo.ts`**：无 segments（单段/无标题）→ 主资产规则仍能给单资产 demo 打标签（验证 demo 不退化）。
6. **新增 `npm run reparse`**（`prisma/reparse.ts`）：扫 `analysis.reviewStatus = needs_review` 且有 `rawText` 的文章，配 Key 时用 LLM 重跑并更新 tag/方向。无 Key 则空跑。
7. **验证**：`db:push` → 重建 → `ingest --slug=j-p-morgan` 看 JPM 只保留 XAUUSD 方向、oil/fed/cpi 因无小标题变 unresolved（不打方向）。

---

## 2. 待办：合规采集升级（"尽可能合规地取到文章"）

**边界（红线，任何时候不破）**：只取公开内容、遇 CAPTCHA/人机验证即停、控频 + 守 Crawl-delay、前端只摘要+回链不转全文、尊重 ToS。

1. **Sitemap 适配器（先做，低风险）**：
   - 从 robots 的 `Sitemap:` 或 `/sitemap.xml` 取 URL 列表；按研究路径前缀 + `lastmod` 近 N 天过滤；逐 URL 过 robots 校验 → fetch → `extractArticle` → 清洗闸。
   - 比抓列表页 HTML 稳、合规、负载低。审计表已标注哪些站有 Sitemap。
2. **Playwright 无头渲染（后做，仅对无 Sitemap/RSS 的 JS 站点）**：
   - `npm i -D playwright` + `npx playwright install chromium`。
   - 新增 `src/lib/ingest/render.ts`：`renderHtml(url)` 返回渲染后 HTML；接入 `run.ts` 作为 fetchText 的 JS 兜底。
   - **每请求仍过 robots 校验 + Crawl-delay**；**检测到人机验证页立即放弃该站**（不绕）。
3. 每机构可选 `rssUrl`/`sitemapUrl` 字段回填（schema 已有 rssUrl/sitemapUrl）。

---

## 3. 待办：真实 LLM 解析

- 用户在 `.env` 配 `ANTHROPIC_API_KEY`（勿粘贴到对话）。
- `parseLLM.realParse` 已就绪（强制 JSON + schema 校验 + 重试 + 降级 needs_review）。
- 配 Key 后跑 `npm run reparse` 回填 needs_review 文章；抽查 tag/方向/摘要质量。

---

## 4. 待办：Phase 3（产品纵深）

- **Forecast Accuracy + Institution Score**：需接价格数据源 + 到期结算逻辑（预测 vs 实际）。机构页杀手锏。
- **Consensus Index 趋势曲线**：已有 `consensus_history` 快照，纯前端可视化，成本最低，可先做。
- **公开 API + API Key**：面向 Professional 档。
- **计费**：需用户 Stripe 账号；先搭 tier-gate 脚手架。

---

## 5. 待办：生产化

- **Postgres + pgvector**：`schema.prisma` datasource `sqlite→postgresql`；JSON 字段 `String→Json`；加向量列。查询层基本不改。docker-compose 起 pg。
- **采集 worker 独立化**：`src/lib/ingest/` 抽成队列 + cron worker。
- **鉴权升级**：现为 MVP 签名 Cookie；换 Auth.js/OAuth；按档位强制权限。
- **测试**：对纯函数加单测（consensus 数学、hash/dedup、robots 匹配、research 意图路由、isJunk）。
- **部署**：Dockerfile + 环境变量。

---

## 6. 关键约束 / 坑（务必记住）

- **合规**：只采 `crawlPolicy ∈ {allowed, delayed}` 的 59 家；blocked/manual（janus/blackrock/neuberger-berman/mizuho/of-america）**永不采**、不绕人机验证。robots 会变 → 定期重跑 `robots_audit.py`。
- **SQLite 文件在 `prisma/dev.db`**（Prisma 按 schema 目录解析相对路径，非仓库根）。
- 改 schema + `prisma generate` 后**必须重启 dev server**（旧 client 缓存内存）。
- 机构 slug 是派生的（JPMorgan → `j-p-morgan`；BMO → `bmo`；Bank of America → `of-america`）。
- 多次热更新后 JS chunk 可能 `ERR_CONNECTION_CLOSED` → 重启 dev server。

---

## 7. 快速恢复指令

```bash
# 全新初始化
npm install
npm run db:push          # 含新 rawText 字段
npm run db:seed          # 64 家 + 资产 + 回填 crawlPolicy
npx tsx prisma/demo.ts   # demo 研报
npm run alerts           # demo 用户 + 规则
npm run dev

# 合规重审 / 采集
python scripts/robots_audit.py
npm run ingest -- --limit=2          # 优先源（仅合规）
npm run ingest -- --all --limit=3    # 全部合规源
npm run consensus                     # 重算快照 + 评估告警

# 待建
npm run reparse          # (TODO) 用 LLM 重跑 needs_review
```

**恢复入口**：从 §1「Segment-aware 解析器 · 待完成」第 1 步继续。
