# Tline（Institutional Intelligence）项目架构与路径总览

> 扫描日期：2026-08-29 · 扫描范围：`E:\Codes\Tline` 全仓（排除 `node_modules`、`.next`、`.git` 内部文件）
> 主线：**Research → Data → Consensus → Signal**

## 1. 项目概览

一个面向机构研究内容的智能平台：合规采集 64 家金融机构公开、免登录的研究内容，清洗后永久保存英文原文，生成可追溯的中文译文与中英 PDF，把每篇文章结构化为「资产 · 方向 · 目标价 · 置信度 · 时间范围」信号，再聚合为按机构权威性加权、随时间衰减的 Institutional Consensus 评分（0–100），并通过 Next.js Web 界面提供研报浏览、搜索、Watchlist、Alerts 与预测准确度披露。

| 项 | 值 |
|---|---|
| 项目名（npm） | `institutional-intelligence` v0.1.0 |
| 语言 | TypeScript（Node 20），另有 1 个 Python 审计脚本 |
| Web 框架 | Next.js 14（App Router，Server Actions，React 18） |
| 数据层 | Prisma 5 · 开发 SQLite（`prisma/dev.db`）/ 生产 PostgreSQL 16 |
| 鉴权 | Auth.js (next-auth) · 生产 Azure AD / Google OAuth · 本地 demo 邮箱直登（默认关） |
| LLM | 可插拔 Provider：Anthropic / OpenAI / DeepSeek（无 Key 时使用确定性规则解析器） |
| 文档 | pdf-lib / pdfkit / pdfjs-dist（PDF 提取、生成、版式翻译） |
| 浏览器 | playwright-core（仅用于公开 JS 页面正常渲染兜底） |
| 测试 | Node 内置测试运行器 + `tsx`（`npm test`） |
| 源码规模 | `src/` 共 7683 行（含测试） |

## 2. 顶层目录结构

```
E:\Codes\Tline\
├── README.md                     # 项目说明（MVP 现状、命令、布局）
├── ARCHITECTURE.md               # 技术架构文档（当前 / 目标）
├── DEPLOYMENT.md                 # 部署、备份、鉴权、结算运维文档
├── plan20260827.md               # 产品总计划（里程碑、验收标准）
├── 20260829ST.md                 # 本文档
├── 64机构爬虫合规评估.xlsx        # robots 离线审计结果（64 家）
├── 全球机构免费研究源清单_64家.xlsx # 64 家来源清单（15 家一期优先）
├── _seed.json                    # 种子数据（机构 / 资产词典）
├── package.json / package-lock.json / tsconfig.json / next.config.mjs / next-env.d.ts
├── .env / .env.example           # 环境变量（.env 不入库）
├── Dockerfile / docker-compose.yml / .dockerignore / .gitignore
├── src/                          # 全部应用源码（App Router + 业务库）
├── prisma/                       # 数据模型、迁移、种子与各处理 CLI 入口
│   ├── schema.prisma             # 主 schema（SQLite dev）
│   ├── postgresql/               # 生成的 PostgreSQL schema + 版本化迁移（7 个）
│   ├── dev.db / crawl-validation.db  # 本地数据库文件
│   ├── seed.ts / demo-user.ts / documents.ts / forecasts.ts / prices.ts
│   ├── reparse.ts / translate.ts
├── scripts/                      # 调度器、环境校验、schema 生成、robots 审计
├── data/                         # 来源注册表、爬取策略、术语库、探测报告、抓取缓存
├── storage/                      # 私有文档存储（本地驱动；生产可切 S3）
│   ├── articles/                 # 正式文档（136 个文件，按文章 ID 分目录）
│   ├── qa/ qa2/ qa3/             # 质量验收样例（共 7 个文件）
│   ├── quarantine/               # 隔离/拒绝文件（27 个）
│   └── .trash/                   # 软删除回收（2 个）
├── tmp/                          # 临时文件（pdfs、sheet-audit）
├── .claude/                      # 本地编辑器配置（launch.json）
└── .next/                        # Next.js 构建产物（不入库）
```

## 3. 系统架构分层

```
┌────────────────────────── Web 层（src/app）──────────────────────────┐
│ 页面：Home / asset / institution / research / markets / institutions │
│       consensus / watchlist(含 alerts 重定向) / signin               │
│ API：auth([...nextauth]) / documents / health / search               │
│ Server Actions：actions.ts（watchlist、alert 规则 CRUD + 即时触发）    │
└───────────────┬──────────────────────────────────────────────────────┘
                │ 查询层 queries.ts / search.ts / viewRanking.ts
                │ 发布闸门 publication.ts · 权限管理器 permissions.ts
┌───────────────▼──────────── 领域库层（src/lib）──────────────────────┐
│ consensus.ts（加权共识+快照） forecast.ts（预测结算） alerts.ts（规则）│
│ i18n.ts（中英切换） audit.ts（审计日志） jobs.ts（JobRun） auth.ts     │
└───────┬────────────────────┬───────────────────┬─────────────────────┘
        │                    │                   │
┌───────▼───────┐   ┌────────▼────────┐   ┌──────▼─────────┐
│ 采集管道       │   │ AI 处理层        │   │ 文档层          │
│ src/lib/ingest│   │ llm/ translation│   │ documents/     │
│ robots→发现→  │   │ parseLLM 结构化  │   │ 提取/生成/版式  │
│ 抓取→提取→闸门 │   │ 翻译+质量复核    │   │ 翻译/安全校验   │
│ →去重→落库    │   │ atomicViews 原子 │   │ storage 适配器  │
└───────┬───────┘   │ 观点            │   │ local / S3     │
        │           └────────┬────────┘   └──────┬─────────┘
        │                    │                   │
┌───────▼────────────────────▼───────────────────▼─────────┐
│ 数据层：Prisma（SQLite dev / PostgreSQL 16 生产）           │
│ 内容域 · 双语文档域 · 信号域 · 用户域 · 运维域 共 21 个模型  │
└────────────────────────────────────────────────────────────┘
        ▲
┌───────┴─────────────── 调度层 ───────────────────────────────┐
│ scripts/scheduler.mjs：每 6h 一次 ingest → reparse →        │
│ translate → documents，有限重试 + 可选失败 webhook           │
└──────────────────────────────────────────────────────────────┘
```

### 3.1 Web 层（`src/app/`）

| 路径 | 说明 |
|---|---|
| `page.tsx` | 首页：Market Consensus + 最新研报 Feed |
| `asset/[ticker]/page.tsx` | 资产页：共识趋势 + 相关研报 |
| `institution/[slug]/page.tsx` | 机构页：研报列表 + 机构视图 |
| `institution/[slug]/accuracy/page.tsx` | 机构预测准确度披露页 |
| `research/page.tsx` · `research/[id]/page.tsx` | 研报列表 / 双语研报详情 |
| `markets/page.tsx` | 市场总览 |
| `institutions/page.tsx` | 机构索引（7 天热度加权排序） |
| `consensus/page.tsx` · `consensus/[ticker]/page.tsx` | 共识总览 / 单资产共识 |
| `watchlist/page.tsx` | 统一监控中心（资产/机构/主题/规则/触发历史） |
| `alerts/page.tsx` | 旧入口，重定向至 watchlist |
| `signin/page.tsx` | 登录页（demo 邮箱 / OAuth） |
| `layout.tsx` · `globals.css` · `error.tsx` · `not-found.tsx` | 根布局与全局样式 |
| `actions.ts` | Server Actions：规则 CRUD、即时评估 |
| `api/auth/[...nextauth]/route.ts` | Auth.js 路由 |
| `api/documents/[id]/route.ts` | 受权限管理的文档下载路由 |
| `api/health/route.ts` | 健康检查（DB + 存储 + 来源状态） |
| `api/search/route.ts` | 站内模糊搜索 API |
| `_components/` | `SearchBox`、`MonitoringRuleForm`、`LanguageToggle`、`ThemeToggle`、`OAuthSignOutButton`、`ui.tsx` |

### 3.2 领域库层（`src/lib/`）

| 文件 | 职责 |
|---|---|
| `queries.ts` | 页面查询层（Prisma 结构化读取） |
| `search.ts` (+test) | 全站模糊搜索（别名、拼写容错、键盘导航） |
| `viewRanking.ts` (+test) | 机构视图排序（7 天热度 → 权威度 → 指数衰减新鲜度） |
| `consensus.ts` (+test) | 共识引擎：`score=(raw+2)/4×100`，权威加权 + 24h 窗口 + 时间衰减 + `ConsensusHistory` 快照 |
| `forecast.ts` (+test) | 预测结算（方向正确性、目标误差） |
| `alerts.ts` (+test) | 告警规则：`CONSENSUS_ABOVE/BELOW/DROP_24H/RISE_24H`、新研报规则 |
| `publication.ts` (+test) | 发布闸门：英文原文 + 英文 PDF 就绪才公开；中文优先展示 |
| `permissions.ts` (+test) | 统一权限管理器（页面/下载/API 共用） |
| `i18n.ts` (+test) | 中英文语言切换 |
| `auth.ts` · `auth-config.ts` | Auth.js 配置（azure-ad / google / demo email） |
| `user.ts` | 用户查询 |
| `audit.ts` | AuditLog 写入 |
| `jobs.ts` | JobRun 任务记录 |
| `hash.ts` | URL/标题/正文三重 hash 去重 |
| `db.ts` | Prisma Client 单例 |
| `assets.ts` | 资产词典工具 |

### 3.3 采集管道（`src/lib/ingest/`）

| 文件 | 职责 |
|---|---|
| `run.ts` | 采集主入口（CLI：`--all` / `--slug=` / `--limit=` / `--attempt=`），限当月内容，来源失败隔离 |
| `probe.ts` | 只读覆盖率审计（`ingest:probe`），不写库、不改来源状态 |
| `robots.ts` (+test) | 运行时 robots.txt 判断（UA 分组、最长匹配、通配符、Crawl-delay） |
| `sitemap.ts` (+test) | Sitemap / Sitemap Index 递归发现与过滤 |
| `fetch.ts` | 受控抓取（限速、退避、Retry-After；PDF 独立二进制下载器） |
| `extract.ts` (+test) | 正文清洗、段落密度正文识别、小标题分段 |
| `parseLLM.ts` (+test) | 规则解析优先 + LLM 补全（ticker 白名单、方向枚举、schema 校验） |
| `store.ts` | 质量闸门（`isJunk`/`looksLikeArticle`）、三重去重、事务化落库 |
| `sourceRules.ts` | 已验收来源的固化例外配置（列表入口、路径、拒绝条件、requiresRender） |
| `render.ts` | Playwright 公开页面正常渲染兜底 |
| `recompute.ts` | `consensus` CLI 入口（重算 + 快照 + 告警评估） |
| `atomicViews.ts` (+test) | 双语原子观点分解（须保留原文引用，弱化条件性语言被拒） |

### 3.4 AI 处理层

| 路径 | 职责 |
|---|---|
| `src/lib/llm/provider.ts` (+test) | Provider 抽象：Anthropic（SDK）/ OpenAI（Responses API）/ DeepSeek（Chat Completions），`getLLMProvider` + `completeJSON` |
| `src/lib/llm/types.ts` | `LLMProvider`、输入输出类型 |
| `src/lib/translation/translate.ts` (+test) | 分段翻译管线（术语库注入、数字/ticker/段落完整性校验、独立 Reviewer 复核） |
| `src/lib/translation/quality.ts` (+test) | 翻译质量闸门（数字/百分比/日期/ticker 100% 一致、术语库命中） |

### 3.5 文档层（`src/lib/documents/`）

| 文件 | 职责 |
|---|---|
| `storage.ts` (+test) | `DocumentStorage` 接口（put/get/exists/signedDownloadUrl），local 适配器 |
| `s3Storage.ts` (+test) | S3 兼容私有存储适配器（30–900 秒签名 URL） |
| `pdf.ts` (+test) | 统一研究模板的英/中 PDF 生成 |
| `extractPdf.ts` (+test) | 原生 PDF 文本/结构提取（born-digital / scanned 判别） |
| `nativeLayout.ts` (+test) | 版式保真中文回填（保持页面尺寸、图位、文本块位置） |
| `pdfSafety.ts` (+test) | PDF 安全校验（签名、大小上限、加密、活动内容/嵌入附件） |

## 4. 数据模型（`prisma/schema.prisma`，共 21 个模型）

| 域 | 模型 |
|---|---|
| 内容域 | `Institution`（64 家来源、crawlPolicy、lastCrawl* 状态）· `Article`（urlHash/titleHash/contentHash 去重，rawText 永久英文原文）· `ArticleSegment`（分段）· `Analysis`（摘要/论据/数字/风险，记录 provider/model/promptVersion/reviewStatus）· `ArticleAsset`（方向 -2..+2、目标价、时间范围）· `Asset`（ticker/别名/资产类别）· `AtomicView`（双语原子观点，含 sourceQuote 引用） |
| 双语文档域 | `ArticleTranslation`（locale、质量分、状态 translated/reviewed/needs_review）· `ArticleTranslationSegment` · `ArticleDocument`（kind: source_native/original_pdf/translation_pdf，storageKey 元数据 + 状态） |
| 信号域 | `ConsensusHistory`（0–100 快照 + 多空计数）· `Forecast`（pending/settled 预测结算）· `PriceObservation`（价格观测导入） |
| 用户域 | `User`（role 与商业 tier 分离）· `Account` · `Session` · `VerificationToken` · `WatchlistItem`（asset/institution/theme）· `AlertRule`（scopeKind/scopeRef 新字段 + assetTicker 旧兼容）· `AlertEvent` · `AuditLog` |
| 运维域 | `JobRun`（任务参数、重试、指标、错误） |

生产 PostgreSQL schema 由 `prisma/schema.prisma` 经 `scripts/generate-postgres-schema.mjs` 生成，迁移位于 `prisma/postgresql/migrations/`（7 个版本：initial → source_status → oauth → forecast_settlement → monitoring_scope → atomic_views → bilingual_analysis）。

## 5. 数据与存储目录

| 路径 | 内容 |
|---|---|
| `data/institutions.json` | 64 家机构注册表（稳定入口、rating、priority、RSS/Sitemap） |
| `data/crawl_policy.json` | robots 离线审计结论（allowed/delayed/blocked/manual + Crawl-delay）；58 家可采、4 家 manual（BlackRock/Neuberger/Mizuho/BofA） |
| `data/financial_glossary.zh-CN.json` | 版本化金融术语库（翻译注入） |
| `data/market-events.json` | 活跃市场事件（机构视图热度） |
| `data/crawl-probe*.json` | 历次只读探测报告 |
| `data/cache/http/` · `data/cache/robots/` | 抓取缓存（571 个 HTTP JSON / 57 个 robots 缓存） |
| `storage/articles/<articleId>/` | 每篇文章的 PDF（original_pdf-en / source-native-en / translation_pdf-zh-CN，共 136 个文件） |
| `prisma/dev.db` | 开发数据库（SQLite） |
| `prisma/crawl-validation.db` | 爬取验证数据库 |

## 6. 命令入口（`package.json` scripts）

| 命令 | 作用 |
|---|---|
| `npm run dev / build / start / test` | Next 开发 / 构建 / 生产 / 测试（`tsx --test src/**/*.test.ts`） |
| `npm run setup` | db push + seed + demo 用户，一次性本地初始化 |
| `npm run db:push / db:seed / db:studio` | Prisma 建库 / 种子（64 机构 + 资产词典）/ Studio |
| `npm run db:postgres:schema / db:postgres:deploy` | 生成 PostgreSQL schema / 执行迁移部署 |
| `npm run ingest / ingest:probe` | 合规实时采集 / 只读覆盖率审计 |
| `npm run reparse / translate / documents` | 补解析 / 补翻译 / 重建 PDF（均支持 `--limit`、`--retry-review`、`--all`） |
| `npm run consensus` | 重算共识 + 快照 + 评估告警 |
| `npm run forecasts / prices:import` | 预测结算 / CSV 价格观测导入 |
| `npm run alerts` | 演示用户 watchlist + 规则种子与触发 |
| `npm run scheduler` | 常驻调度器（`scripts/scheduler.mjs`） |
| `npm run env:check / env:check:production` | 环境变量校验 |
| `python scripts/robots_audit.py` | 64 家 robots.txt 离线审计 → Excel + crawl_policy.json |

## 7. 部署架构（Docker Compose，3 个服务）

| 服务 | 镜像 | 职责 |
|---|---|---|
| `db` | postgres:16-alpine | 生产数据库（库 `tline`，命名卷 `postgres-data`，pg_isready 健康检查） |
| `app` | 本仓 Dockerfile（node:20-bookworm-slim，内置 Noto CJK/Liberation 字体） | Web 服务；启动先 `env:check:production` → `db:postgres:deploy` → `next start`；健康检查 `/api/health` |
| `scheduler` | 同镜像 | 单实例采集调度：启动即跑一次，此后每 `INGEST_INTERVAL_MS`（默认 6h）执行 ingest → reparse → translate → documents；`JOB_RETRY_LIMIT`（默认 3）有限重试 + 可选 `JOB_FAILURE_WEBHOOK_URL` |

共享卷：`postgres-data`（数据库）、`document-storage`（私有文档，app 与 scheduler 共用，映射 `/app/storage`）。文档存储通过 `DOCUMENT_STORAGE_DRIVER`（local / s3）切换，S3 下载先鉴权再签发 30–900 秒签名 URL，绝不公开永久地址。

## 8. 核心数据流

1. **采集**：scheduler → `ingest run.ts` → robots 运行时判断 → RSS/Sitemap/列表页/PDF 发现 → 受控抓取（限速、当月窗口）→ `extract.ts` 清洗分段 → 质量闸门 + 三重 hash 去重 → 事务写入 `Article`/`Analysis`/`ArticleAsset`；每来源记录 `lastCrawlStatus`，全局记录 `JobRun`。
2. **结构化**：`reparse.ts` → 规则解析器（无 Key 也可跑）→ unresolved 时 LLM 补全（ticker 白名单 + schema 校验）→ `AtomicView` 双语原子观点（必须带原文引用）。
3. **双语化**：`translate.ts` → 分段翻译（术语库注入、数字/ticker 完整性校验、独立 Reviewer）→ 质量闸门 → `ArticleTranslation`；`documents.ts` → 英/中 PDF 生成或原生 PDF 版式翻译 → `ArticleDocument`。
4. **信号**：`consensus recompute.ts` → 24h 窗口权威加权共识 + `ConsensusHistory` 快照 → 告警规则评估。
5. **消费**：Web 页面经 `queries.ts` 读取；发布闸门 `publication.ts` 决定可见性；下载经 `permissions.ts` + `api/documents/[id]` 路由。

## 9. 关键环境变量（`.env.example`）

- 数据：`DATABASE_URL`（SQLite dev / PostgreSQL 生产）
- LLM：`LLM_PROVIDER` + `ANTHROPIC/OPENAI/DEEPSEEK_API_KEY/MODEL`；未配置时确定性解析器，翻译安全等待
- 文档：`DOCUMENT_STORAGE_DRIVER`、`DOCUMENT_STORAGE_ROOT`、S3 系列、`PDF_FONT_*`（中英字体）
- 鉴权：`AUTH_SECRET`、`AUTH_PROVIDER`（azure-ad / google）、`NEXTAUTH_URL`、OAuth 凭据、`ALLOW_INSECURE_DEMO_AUTH`（生产默认关闭）
- 调度：`INGEST_INTERVAL_MS`、`INGEST_ARTICLE_LIMIT`、`PROCESS_ARTICLE_LIMIT`、`JOB_RETRY_*`
- 结算：`PRICE_SETTLEMENT_TOLERANCE_DAYS`（默认 7）

## 10. 测试与既有文档

- 测试文件与源码同目录：`src/lib/**/*.test.ts`（robots、sitemap、extract、parseLLM、consensus、alerts、forecast、i18n、permissions、publication、search、viewRanking、translation/*、documents/*、llm/provider、ingest/atomicViews）。
- 文档体系：`README.md`（运行现状与命令）、`ARCHITECTURE.md`（架构决策与目标）、`DEPLOYMENT.md`（生产部署与运维边界）、`plan20260827.md`（产品里程碑）、`64机构爬虫合规评估.xlsx`（robots 审计）、`全球机构免费研究源清单_64家.xlsx`（来源清单）。
