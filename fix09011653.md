# Fix 2026-09-01 16:53 — 首页"置顶卡死" + 视频/音频/直播类内容混入

## 现象
- 首页「最新机构观点」区块,一篇 **Nordea Economic Outlook September 2026** 长期挂在最顶,推不下去。
- 该内容本应被"视频、音频、博客类不要"的规则挡在门外,却进来了。

## 根因(两个问题,同一个源头)
这篇 Nordea 页面**根本不是研报,是一个网络研讨会(webinar)报名页**。正文原文:
> "Join Helge J. Pedersen as he explores… **Date: 2 September 2026 · Time: 11:00 CET · Duration: 30 minutes plus Q&A · Language: English**"

1. **置顶**:日期推断把正文里的活动时间 "Date: 2 September 2026" 当成了 `publishedAt` → 存成 **未来日期(9-02,明天)**。首页 feed(`latestFeed`)按 `publishedAt` 倒序排,未来日期永远压顶。
   - `run.ts:147` 原有的未来日期护栏是 `> now + 864e5`(**放行 24 小时内的未来**),而这个活动只领先约 18 小时,**从护栏下面钻过去了**。
   - 显示用的是 `articleTimestamp`(把未来日期钳到发现时间,所以卡片显示"6小时前"),**排序却用原始未来 `publishedAt`** → 两套逻辑不一致,才出现"显示不新、却卡顶"。
2. **内容类型**:流水线里**根本没有 视频/音频/直播/博客 的类型过滤**。`extract.ts` 里唯一相关的匹配是把 `BlogPosting` 当作合法正文**接受**。webinar 页正文含经济/通胀词,轻松通过 `looksLikeResearchTopic` + `looksLikeArticle`。

## 修复方案

### A. 新增广播/活动类型过滤(核心,对应"视频/音频不要")
`src/lib/ingest/extract.ts` 新增 `isBroadcastOrEvent(title, text)`:
- 命中显式媒体词(webinar / webcast / livestream / podcast / episode / on-demand video / watch the replay / listen to the episode …)→ 判为广播;
- 或 **同时命中**"排期"(`Duration: N minutes` 或 `Time: HH:MM <时区>`)**和**"报名 CTA"(register/save your seat/join … as he/she/they …)→ 判为活动邀请。
- Nordea 例:`Duration: 30 minutes` + `Join Helge … as he explores` → 命中,拒收。

### B. 未来日期钳制(防止任何未来日期卡顶,纵深防御)
`src/lib/ingest/run.ts` 的 `stage()` 单一收口处:
- 保留 `> now + 24h` 的荒谬未来(解析错误)直接拒收;
- 新增:`publishedAt > now` 的**近未来**(时区偏差 / 活动日期误当发布日)→ **钳到 now**。"页面不可能在我们发现它之后才发布",与显示端 `articleTimestamp` 的口径一致。

### C. 接线
`run.ts` 的 `stage()` 内,在 `articleAllowed` 之后、`strict` 判定之前加:
`if (isBroadcastOrEvent(raw.title, raw.text)) { empty++; return false; }`
并在 import 里加入 `isBroadcastOrEvent`。

### D. 测试
`src/lib/ingest/extract.test.ts` 新增用例:
- 真实 Nordea webinar 文本 → `true`;podcast / on-demand webcast → `true`;
- 提到时长/时间但是真研报的正文 → `false`(防误杀)。

## 数据清理(代码修完后)
- 删除库里已入库的 Nordea webinar 文章(id `cmti05y0n000rykmozgduz3ye`)及其派生(translation / segments / documents / atomicViews)。
- 扫描存量文章中命中 `isBroadcastOrEvent` 的其它条目,列出、确认后清除。
- 复查:当前未来日期文章仅此 1 篇(已确认),清掉后 feed 顶部恢复正常。

## 验证
1. `npx tsc --noEmit` 干净;`npx tsx --test src/lib/ingest/extract.test.ts` 全绿;`npm test` 全绿。
2. 删除该文章后,首页 feed 顶部不再是这篇 Nordea;`publishedAt > now` 的文章数为 0。
3. 复跑一次 Nordea 定向 ingest(或 probe),确认该 webinar 页不再被采纳。

## 影响文件(均非并行会话在改的文件)
- `src/lib/ingest/extract.ts`(+ `isBroadcastOrEvent`)
- `src/lib/ingest/run.ts`(接线 + 近未来日期钳制)
- `src/lib/ingest/extract.test.ts`(新增用例)

## 提交
独立一次提交:`fix(ingest): reject webinar/broadcast pages and cap future publish dates`。
不触碰工作区里并行会话的在途改动(scheduler / docker / admin / health / natixis 等)。
