# Institutional Intelligence · Master Plan

> **Research → Data → Consensus → Signal**
> Product, data, and delivery plan · 2026-08-27 · Living document

本文件是项目后续执行的唯一主计划。Claude 留下的 `design-spec.html` 提供产品与视觉基线；本次确认补齐数据采集、双语翻译、PDF、质量控制和权限架构。

## 00 · Decision Order — 决策优先级

发生冲突时严格按以下顺序执行：

1. 用户当前明确要求。
2. 后续与用户共同确认的决定。
3. Claude 的产品设计规范。
4. 本计划、`ARCHITECTURE.md` 与旧实现。

已知覆盖项：Claude 设计中“研报详情不保存/不提供全文”的旧设想不再适用。系统必须永久保存清洗后的英文正文与中文译文，并为每篇文章提供英文、中文 PDF；权限管理器统一控制访问，具体套餐矩阵后定。

---

## 01 · Product Thesis — 产品命题

用户不需要再手工阅读、比对几十页机构报告。Tline 将公开机构研究转化为可追溯的结构化信号：

```text
公开研究 → 合规采集 → 英文事实源 → 金融级中文译文
        → 资产观点 → 机构共识 → 异动信号 → 搜索 / 关注 / 告警
```

### 体验目标

- **60 秒每日巡场**：Home 扫共识 → View Changes 找异动 → Asset 查看机构变化 → Watch。
- **单资产研究**：搜索资产 → 共识/目标价/观点链 → 相关研报 → 中英正文/PDF/官网证据。
- **可信 AI 检索**：只基于站内已采集证据回答，答案带编号引用，不开放式重搜互联网。
- **持续监控**：Watchlist + Alerts 将观点变化主动送到用户面前。

### 不做的事

- 不采集登录墙、付费墙或客户专属内容。
- 不绕过 CAPTCHA、WAF、人机验证或其他访问控制。
- 不把翻译、摘要或模型推断伪装成机构原文。
- 不因“可能提到某资产”就污染 Consensus；不确定信号必须隔离。
- 初期不引入微服务、队列、复杂权限套餐和像素级 PDF 复刻。

---

## 02 · Visual Language — 视觉语言

Claude 设计基线：**Bloomberg × Linear × Perplexity**。

- 背景像纸：白色/浅灰，大面积留白，边框极细。
- 高信息密度，但不依赖装饰性卡片堆叠。
- 颜色只表达信号：Cobalt `#2f55d4`、Bull `#0e9a6c`、Bear `#d22d2d`、Neutral `#b7791f`。
- 字体：Newsreader 用于标题与编辑感；Public Sans 用于 UI；IBM Plex Mono 用于 ticker、数值和状态。
- 方向永远同时使用颜色、箭头和文字，不能只靠颜色。
- Desktop-first；`≤940px` 单栏/导航折叠，`≤560px` 网格降至 1–2 列；表格自身横滚，body 不横滚。
- 动效服务于状态变化，支持 `prefers-reduced-motion`，不做持续或装饰性动画。

---

## 03 · Screen Map — 页面地图

### Built / Existing — 已有基础页面

| ID | Route | Purpose | Current state |
|---|---|---|---|
| P1 | `/` | Home：Consensus + Feed + View Changes | Built，待真实数据验证 |
| P2 | `/markets/:class?` | 按资产类别浏览共识 | Built |
| P3 | `/asset/:ticker` | 单资产共识、机构观点、目标链 | Built |
| P4 | `/institutions` | 64 家机构与优先来源 | Built |
| P5 | `/institution/:slug` | 机构当前观点与研报 | Built |
| P6 | `/research` | 研报流 | Built，待筛选/分页 |
| P7 | `/research/:id` | 研报详情与证据 | Built，需升级为双语/PDF |
| P8 | `/consensus` | 全资产共识总览 | Built |
| P9 | `/search` | 站内 AI Research | Built，待多模型统一 |
| P10 | `/watchlist` | 关注资产/机构 | Built · MVP Auth |
| P11 | `/alerts` | 共识阈值与异动规则 | Built · MVP Auth |
| P12 | `/signin` | MVP 邮箱会话 | Built |

### Planned — 规划页面

| ID | Route | Purpose | Priority / dependency |
|---|---|---|---|
| P13 | `/consensus/:ticker` | Consensus Index 趋势 | 高；已有历史快照，先做 |
| P14 | `/institution/:slug/accuracy` | 预测准确率与机构评分 | 后置；依赖价格源和结算 |
| P15 | `/pricing` | 套餐价值与功能对比 | 权限矩阵确定后 |
| P16 | `/account` | 资料、订阅、通知、API Key | 依赖正式鉴权 |
| P17 | `/developer` | Professional REST API | 依赖 Key、限流、计量 |

### Research Detail — 用户决策后的新版信息架构

`/research/:id` 按以下顺序呈现：

1. 机构、作者、发布时间、来源、数据质量状态。
2. 中英文标题与语言切换。
3. AI Summary（明确标记为模型生成）。
4. Core Views、Key Numbers、Arguments、Risks、Trading Interpretation。
5. 完整中文译文与英文原文入口；展示资格由权限管理器判断。
6. 下载：英文 PDF、中文 PDF；来源原生 PDF 另保留原文件身份。
7. 官网原文回链、采集时间、翻译模型/版本、质量状态。

---

## 04 · System Blueprint — 系统蓝图

```text
Institution Registry
  └─ Robots Policy (24h last-known-good)
      └─ Discovery: RSS → Sitemap → HTML → public JSON → Render
          └─ Fetch: HTML / native PDF
              └─ Extract + Segment + Quality Gate + Dedup
                  ├─ English canonical article
                  ├─ Asset parse → Consensus / Alerts
                  ├─ Translation → QA → Chinese article
                  └─ Documents → EN PDF / ZH PDF / native PDF

Prisma DB ── Query layer ── Next.js pages / Server Actions / Download routes
Private file storage ────── Permission manager ───────────────────────┘
```

### 代码边界

- `src/lib/ingest/`：发现、robots、抓取、提取、解析、落库。
- `src/lib/llm/`：统一 Provider、schema 校验、重试、模型选择。
- `src/lib/translation/`：术语库、翻译、数字检查、复核与重译。
- `src/lib/documents/`：原生 PDF 下载、正文 PDF 生成、中文 PDF 迁移和验证。
- `src/lib/permissions.ts`：页面、Server Actions、下载和 API 的统一授权入口。
- `src/lib/consensus.ts`：只消费已通过质量门的英文结构化观点。
- `src/app/`：渲染与交互，不承载采集/翻译业务逻辑。

### 初期部署形态

- Next.js 单体 + Prisma + SQLite。
- CLI/定时任务复用同一代码库。
- 文件存储先用可替换的本地私有目录实现，接口兼容后续 S3/R2。
- 出现真实吞吐和隔离需求后再迁 PostgreSQL、独立 Worker/队列。

---

## 05 · Canonical Data — 数据事实源

### 永久保存

- 清洗后的英文标题、正文、段落结构。
- 完整中文标题、译文、段落结构。
- 原始来源 URL、机构、作者、发布时间、抓取时间、hash。
- 英文 PDF、中文 PDF；来源自带 PDF 时保存原始二进制。
- 解析、翻译、复核所用 provider、model、prompt、glossary 和版本。
- 结构化观点、目标价、置信度、Consensus 历史及质量状态。

### 明确边界

- 英文正文是事实源；翻译与摘要是可重建派生数据。
- Consensus 只能由英文事实源解析产生，不能由译文反推。
- `rawText` 指已经去导航、页脚、Cookie 条等样板后的完整正文，不是未经筛选的网页 HTML。
- 当前永久保留；未来若调整保留策略，必须先做迁移/备份方案，不静默删除。

### 目标模型

- `Article`：英文 canonical 内容及来源元数据。
- `ArticleSegment`：稳定顺序、heading、英文段落。
- `ArticleTranslation`：`zh-CN` 当前有效译文及质量/版本。
- `ArticleTranslationSegment`：与英文 segment 一一对应。
- `ArticleDocument`：`source_native` / `original_pdf` / `translation_pdf`，含路径、hash、页数、状态和错误。
- `Analysis`：摘要、论点、风险、模型与复核状态。
- `ArticleAsset`：只有已解决、可进入 Consensus 的资产观点。

---

## 06 · Translation Standard — 金融级中文翻译

目标不是“通顺的机器翻译”，而是达到机构研究部门可交付的中文水准：忠实、完整、克制、术语统一、数字零损失。

### Pipeline

1. 结构化英文正文，给标题、小标题、段落、表格和脚注分配稳定 ID。
2. 注入机构名、资产名和版本化金融术语库。
3. 主模型按完整上下文翻译，不概括、不删减、不增添观点。
4. 程序检查数字、货币、百分比、基点、日期、ticker、段落数量。
5. Reviewer 对照检查遗漏、误译、语气强弱和术语一致性。
6. 未通过则按错误清单修订一次；仍未通过标记 `needs_review`，不冒充成品。

### Language rules

- 保留 `may / could / likely / risk` 的不确定性。
- `bps` 统一为“个基点”，数值和正负号不得改变。
- 公司、指数、ticker、型号默认保留官方写法，首次出现可补中文名。
- 目标价、区间、时间周期和上调/下调关系必须逐项对应。
- 译文不加入交易建议，不把作者观点改写为平台结论。

### Provider strategy

- 第一批支持 Anthropic 与 OpenAI，业务层只依赖统一接口。
- 解析、翻译、复核可分别配置模型；高价值文章优先交叉 Provider 复核。
- 没有可用 Key 时保留英文并进入待翻译队列，绝不生成伪译文。

---

## 07 · PDF Contract — PDF 交付标准

每篇通过质量门的文章最终必须有：

- `original_pdf`：英文原文 PDF。来源是 HTML 时由系统排版生成；来源是 PDF 时原文件即权威原件。
- `translation_pdf`：完整中文 PDF。
- `source_native`：若网站本身提供 PDF，原样下载并永久保存。

### 来源 HTML

- 使用统一 Tline 研究模板生成中英 PDF。
- 保留标题层级、段落、列表、表格、图片、脚注和来源信息。
- PDF 必须可搜索、可复制；中文字体必须嵌入。

### 来源 PDF

- 原始 PDF 不修改，保存 SHA-256、页数和文件类型。
- 中文版尽量保持分页、栏结构、标题层级、表格/图片位置和脚注对应。
- 不要求逐像素复刻；验收顺序是：**内容完整 > 翻译准确 > 可读性 > 主要版式结构**。
- 扫描版先 OCR；无法可靠恢复的表格或图形保留原图并翻译标题/图注。

### Quality gate

- 文件可打开，页数非零，无空白页、截断、重叠和字体乱码。
- 英文/中文正文段落、数字与 ticker 对应检查通过。
- 生成后逐页渲染为图片做视觉检查；异常文档标记失败并可重建。
- 只有 `status=ready` 的文件可下载。

---

## 08 · Permissions — 权限管理器

当前先实现统一能力判断，不提前锁死套餐：

```ts
can(user, action, resource): boolean
```

首批 action：

- `article.read.original`
- `article.read.translation`
- `document.download.original`
- `document.download.translation`
- `watchlist.manage`
- `alerts.manage`
- `api.use`
- `admin.review`

所有页面、Server Actions、Route Handlers 和未来 REST API 必须调用同一管理器。当前核心开发阶段采用宽松默认策略保障功能可验收；上线计费前再确定 Free / Pro / Trader / Professional 的具体矩阵。

---

## 09 · Delivery Sequence — 实施路线

每个 Milestone 独立编译、测试、提交并推送。后一个阶段不得掩盖前一个阶段的失败。

### M0 · Spec Lock — 规范归档

**Status：COMPLETE**

- [x] 扫描项目、数据库模型、脚本和现有页面。
- [x] 读取 Claude 产品设计与页面矩阵。
- [x] 记录用户覆盖决策。
- [x] 更新 README 和架构文档，使三份文档互相引用且无冲突。

**Exit gate**：产品、技术、数据保留、翻译、PDF 和权限均有唯一书面定义。

### M1 · Signal Integrity — 分段解析

**Status：COMPLETE**

- [x] 提取器输出 heading-aware segments。
- [x] 规则解析器按段识别资产方向与冲突。
- [x] unresolved 资产不写入 `ArticleAsset`。
- [x] `rawText` 和 segments 从抓取传到落库。
- [x] 修正测试/类型错误并完成单元测试。
- [x] 增加可重复执行的 `reparse` 命令。

**Exit gate**：多资产冲突不会污染 Consensus；无模型 Key 仍可安全运行；`npm test`、`tsc`、build 通过。

### M2 · Bilingual Core — 双语数据与多模型

**Status：COMPLETE**

- [x] 建立 `src/lib/llm/` Provider 接口和 Anthropic/OpenAI 实现。
- [x] 让解析与 AI Research 复用 Provider，不直接 import 厂商 SDK。
- [x] 增加 translation/segment/document 数据模型并迁移开发库。
- [x] 建立金融术语库 v1、翻译 prompt v1 和数字完整性检查。
- [x] 实现 translate/retranslate CLI，支持幂等、失败恢复和待复核状态。

**Exit gate**：代表性文章可生成完整中文译文；所有数字/ticker 检查通过；模型/提示/术语版本可追踪。

### M3 · Document Core — 双语 PDF

**Status：COMPLETE**

- [x] 私有文件存储接口 + 本地实现。
- [x] HTML 文章统一生成英文/中文 PDF。
- [x] 原生 PDF 保存、hash、元数据和坐标化文本提取。
- [x] 原生 PDF 中文迁移，保留页面、图片、图表和主要结构。
- [x] 下载 Route Handler + 权限管理器。
- [x] 渲染逐页检查并加入文档状态机。

**Exit gate**：每篇样本文章都有可下载、完整可读的中英 PDF；原生 PDF 原件未被修改；失败文件不会被交付。

### M4 · Discovery Coverage — 合规采集覆盖

**Status：COMPLETE**

- [x] Sitemap / Sitemap Index 发现与 `lastmod` 过滤。
- [x] robots 24h last-known-good 缓存。
- [x] ETag / Last-Modified、Retry-After 和结构化来源日志。
- [x] 只对明确配置的公开 JS 页面启用正常浏览器渲染。
- [x] CAPTCHA、登录墙、WAF 命中即停止，不绕过。

**Exit gate**：代表性 RSS、Sitemap、静态 HTML、JS 页面和 PDF 来源可幂等运行；blocked/manual 永不自动升级或采集。

### M5 · Product Integration — 产品整合

**Status：COMPLETE（2026-08-27）**

- [x] Research Detail 完整接入双语正文、质量状态和四类下载入口。
- [x] Research Feed 增加机构/资产/方向筛选和分页。
- [x] Institutions 展示 crawl policy 与最近成功状态。
- [x] 完成 P13 Consensus Index 趋势页。
- [x] 全站 loading / empty / error / auth / needs_review 状态统一。
- [x] 响应式、键盘焦点、对比度与 reduced motion 验证。

**Exit gate**：四条核心用户流程可从头走通；移动端不产生 body 横滚；用户能追溯每个模型结论。

### M6 · Production Foundation — 生产基础

**Status：PLANNED**

- [ ] PostgreSQL 迁移；需要语义检索时再加 pgvector。
- [ ] 正式 Auth.js/OAuth、账户与审计日志。
- [ ] 定时任务、备份、可观测性、失败告警。
- [ ] 对象存储、签名下载、内容安全与容量策略。
- [ ] 容器化和部署说明。

### M7 · Commercial Layer — 商业化

**Status：DEFERRED**

- [ ] 与用户确认权限矩阵与四档套餐。
- [ ] P15 Pricing、P16 Account、P17 Developer/API。
- [ ] 支付、API Key、限流、计量和批量导出。

### M8 · Forecast Accuracy — 预测准确率

**Status：DEFERRED**

- [ ] 选择合规价格数据源。
- [ ] 建立预测到期、结算、误差和方向准确率规则。
- [ ] P14 Institution Accuracy 与 Institution Score。

---

## 10 · Quality Gates — 全局验收门

任何阶段完成必须同时满足：

- `npm test` 通过。
- `npx tsc --noEmit` 通过。
- `npm run build` 通过。
- 数据迁移可从空开发库重复执行。
- CLI 重跑不会重复写入或破坏已完成数据。
- 错误隔离到单文章/单机构，不拖垮整批任务。
- 密钥、数据库、下载文件和第三方全文不提交 Git。
- 涉及页面时完成桌面/移动布局、空态、错误态和键盘操作检查。
- 涉及 PDF 时完成提取检查和逐页渲染检查。

---

## 11 · Locked Decisions — 已确认决策

- 开发数据可丢弃，当前优先正确模型而非兼容旧数据。
- 只采公开、无需登录的内容；按 robots/访问规则运行，不绕过控制。
- robots 临时不可达可使用 24 小时内 last-known-good；无有效缓存则暂停。
- 永久保存清洗后的英文正文与完整中文译文。
- 所有内容在数据库保存中英文版本，并在文件存储保存中英文 PDF。
- 来源自带 PDF 时原样保存；中文版保持主要布局，不要求逐像素复刻。
- 所有原文必须可作为 PDF 下载，交付通过统一权限管理器。
- LLM 支持多供应商，首批 Anthropic + OpenAI。
- 中文翻译以完整、准确、专业、可复核为目标，不能牺牲事实换取流畅。
- 权限管理器现在实现，具体套餐划分等核心功能完成后再定。

---

## 12 · Resume Point — 中断恢复点

当前从 **M6** 推进。最近执行顺序：

1. 补齐容器化和部署说明，固化当前可复现运行环境。
2. 建立不绑定套餐等级的账户、角色和审计基础。
3. 抽象 PostgreSQL 与对象存储生产配置，同时保留本地 SQLite/文件存储开发路径。
